import { NextFunction, Request, Response } from "express"

const handleCreateMarchent = (req:Request , res:Response , next:NextFunction)=>{

}
export const marchentController = {
    handleCreateMarchent
}